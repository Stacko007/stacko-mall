# Stacko Mall Admin Frontend

商城管理端，基于 React、Vite 和 Ant Design。

```bash
npm install
npm run dev
```

默认端口为 `5174`，`/user` 和 `/mall` 请求代理到 `http://localhost:8088`。

```bash
VITE_GATEWAY_TARGET=http://127.0.0.1:18088 npm run dev
```

构建：

```bash
npm run build
```
