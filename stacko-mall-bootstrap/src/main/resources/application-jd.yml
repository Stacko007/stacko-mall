spring:
  datasource:
    url: jdbc:p6spy:mysql://117.72.247.17:3306/stacko_mall?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
    username: stacko_mall
    password: Zxy@19860122
    driver-class-name: com.p6spy.engine.spy.P6SpyDriver

  data:
    redis:
      host: localhost
      port: 6379
      password: 123456
  flyway:
    enabled: false
    locations: classpath:db/migration
    baseline-on-migrate: true

stacko:
  user:
    persistence: mybatis-plus
    cache: redis
    web:
      swagger-bypass: true
    token:
      strategy: jwt
      jwt:
        secret: change-me
        ttl: PT2H
        exclude-paths:
          - /api/c/products
          - /api/c/products/**
          - /api/c/payments/callback
    bootstrap:
      enabled: true
      tenant-id: platform-root
      username: platform-admin
      password: 123456


logging:
  level:
    com.stacko.user: info
    org.springframework: warn
    org.mybatis.spring.mapper: error
  config: classpath:logback-plus.xml

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.nologging.NoLoggingImpl
    #log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl
